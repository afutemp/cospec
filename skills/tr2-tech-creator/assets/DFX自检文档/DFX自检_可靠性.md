# 可靠性 自检清单

> **版本**: V1.0.7 | **状态**: 待自检 | **更新时间**: 2025

---

## 自检说明

- [ ] 请根据实际产品情况填写"是否满足"列
- [ ] 不满足的条目需在"当前情况"列说明原因和后续计划
- [ ] 完成后请更新"确认人"签名

---

## 硬件可靠性

| 子类 | 具体场景 | 优先级 | 基线 | 适用产品 | 版本 | 是否满足 | 当前情况 | 工作量 | 确认人 |
|------|---------|--------|------|---------|------|---------|---------|--------|--------|
| 硬件狗 | 设备具备硬件看门狗（WDT）机制，当出现死机，看门狗可自动复位或重启设备，复位后自动恢复业务，本地配置不丢失 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |

## OS可靠性要求

| 子类 | 具体场景 | 优先级 | 基线 | 适用产品 | 版本 | 是否满足 | 当前情况 | 工作量 | 确认人 |
|------|---------|--------|------|---------|------|---------|---------|--------|--------|
| 系统备份 | 支持双BOOT备份功能，通过 “双启动分区 + 实时同步” 实现系统级故障快速恢复 | 中 | DFX基线 | 全产线 | V1.0.7 |  |  |  |  |
| OS可靠性要求 | 操作系统宕机： 当系统检测到某主机节点的操作系统发生宕机时，该主机的操作系统运行日志或者黑匣子中能够记录操作系统宕机的详细原因（比如内核BUG、内存耗尽等等），当某个主机上的操作系统宕机的时候，能够主动触发系统自动重启，恢复操作系统的正常运行 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| OS可靠性要求 | 操作系统假死： 当系统检测到某主机节点的操作系统发生挂死时，该主机的操作系统运行日志或者黑匣子中能够记录操作系统挂死的详细原因（比如内核死锁、内存耗尽等等），当某个主机上的操作系统卡死的时候，能够通过硬件狗或者BMC触发系统自动重启，恢复操作系统的正常运行 | 中 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |

## 进程可靠性

| 子类 | 具体场景 | 优先级 | 基线 | 适用产品 | 版本 | 是否满足 | 当前情况 | 工作量 | 确认人 |
|------|---------|--------|------|---------|------|---------|---------|--------|--------|
| 进程守护机制 | 通过独立守护进程 / 容器编排工具，实现进程崩溃、退出后的自动重启，杜绝进程异常终止后无人干预的情况。具体说明参考如下： 1. 单机环境：部署独立于业务进程的守护进程（非业务进程子进程），守护进程权限低于业务进程，避免被业务进程异常牵连； 2. 容器 / 云原生环境：基于 K8s/DC/OS/Swarm 实现进程（容器）守护，配置restartPolicy=Always/OnFailure； 3. 核心要求：业务进程异常退出（退出码非 0）、被杀死后，守护端10 秒内完成自动重启，重启后进程能正常加载配置、连接依赖服务，重启次数单日默认阈值≤10 次（防止死循环重启） | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 多维度故障检测机制 | 从「进程存活、端口 / 套接字可用、业务心跳、进程状态」四层做检测，避免 “进程在运行但业务不可用” 的假活状态。具体说明参考如下： 实现四层故障检测，检测频率≤5 秒，任意一层检测失败连续 2 次即判定进程异常，触发告警并执行恢复动作： 1. 进程存活检测：通过 PID 文件 / 系统指令（ps/top）检测进程是否存在； 2. 网络可用检测：检测进程监听的端口 / UNIX 套接字是否处于 LISTEN 状态，能正常接收连接； 3. 业务心跳检测：进程内置心跳接口（如/health），返回业务层核心状态（如数据库连接、缓存连接、核心线程池可用），检测端调用后判断是否正常； 4. 进程状态检测：检测进程是否处于僵死（Zombie）、无响应（CPU 占用 100%/ 内存持续飙升且无业务操作）状态。 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 分级异常恢复机制 | 区分进程轻量异常（如连接数满）、重度异常（如崩溃 / 死锁），对应采用 “进程重启、节点重启、流量暂切” 的分级恢复策略，减少恢复对业务的影响。具体说明参考如下： 1. 代码层：对所有主线程、子线程、定时任务的执行逻辑增加异常捕获（包括未捕获异常、空指针、IO 异常、网络异常等），捕获后记录详细异常日志，且子线程异常不扩散至主线程； 2. 进程级：注册系统级信号量处理（如 Linux 的 SIGSEGV、SIGABRT、SIGILL），接收到崩溃信号时，先完成 “核心数据落盘、连接释放、异常日志写入”，再按规则退出； 3. 核心要求：异常捕获后不影响进程核心业务逻辑运行，兜底策略为 “降级处理非核心功能，保障核心功能”。 | 中 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 进程资源管控机制 | 为进程设置 CPU、内存、文件句柄、网络连接数的阈值限制与资源回收机制，防止资源耗尽导致进程僵死。具体说明参考如下： 1. 为进程配置CPU、内存、文件句柄（fd）、网络连接数、线程数的硬阈值（基于业务峰值评估），超过阈值时触发 “非核心资源回收 + 告警”； 2. 实现资源自动回收机制：对空闲超 5 分钟的数据库连接、缓存连接、网络连接进行自动关闭，对大对象、临时缓存进行定时清理； 3. 资源阈值触发策略：首次触发阈值→回收资源 + WARN 告警；连续 3 次触发阈值→暂停非核心业务 + ERROR 告警；达到硬阈值→进程优雅重启（避免被系统 OOM 杀死）； 4. 容器环境：结合 K8s 的 Resource Limits/Requests 设置，实现进程与容器的资源联动管控。 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 故障隔离防护机制 | 通过进程级资源隔离、线程池隔离、异常捕获隔离，避免单进程内部异常（如子线程崩溃）扩散导致整个进程挂掉，或单进程故障引发同节点其他进程异常。具体说明参考如下： 1. 进程运行时的核心业务状态、会话数据、临时缓存需实时持久化至本地磁盘 / 分布式存储（如 Redis），持久化失败时触发告警； 2. 进程配置文件支持热加载（无需重启进程），配置更新后 10 秒内生效，且热加载过程中不中断核心业务处理； 3. 进程重启后，能自动从持久化存储中恢复重启前的核心状态与会话数据，恢复时长≤30 秒，且数据无丢失、无错乱。 | 中 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 全链路日志溯源机制 | 记录进程的启动、重启、异常、资源占用、恢复操作等全量日志，实现故障根因快速定位，避免同类故障重复发生。具体说明参考如下： 1. 日志覆盖：进程启动 / 停止 / 重启、配置加载、依赖服务连接、异常捕获、资源占用阈值触发、故障检测结果、恢复操作等全场景； 2. 日志字段：包含时间戳（毫秒级）、进程 PID、操作类型、异常码、详细描述、节点 IP，异常日志需附带堆栈信息； 3. 日志存储：日志输出至本地文件 + 远程日志平台（如 ELK/PLG），本地日志按天切割，保留≥7 天，远程日志保留≥30 天； | 中 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |

## 数据可靠性

| 子类 | 具体场景 | 优先级 | 基线 | 适用产品 | 版本 | 是否满足 | 当前情况 | 工作量 | 确认人 |
|------|---------|--------|------|---------|------|---------|---------|--------|--------|
| 配置文件冗余 | 对于客户不可修改的配置文件，需要有冗余机制，当配置文件损坏后，能够重新初始化成功且记录日志 | 高 | DFX基线 | 全产线 | V1.0.7 |  |  |  |  |
| 数据冗余 | 1. 系统自身的配置数据以及用户的业务配置数据在系统内部至少保存两份，两份之间要保证实时的一致性 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 数据冗余 | 2. 支持将指定数据备份第三方存储设备上，也支持备份到系统内部的存储上，在原始数据发生损坏时使用备份数据进行恢复 | 中 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 数据冗余 | 3. 服务器的系统盘应该支持RAID方式冗余配置，单个系统盘损坏不会导致服务器运行异常，支持系统盘热拔插方式进行更换 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |

## 业务可靠性

| 子类 | 具体场景 | 优先级 | 基线 | 适用产品 | 版本 | 是否满足 | 当前情况 | 工作量 | 确认人 |
|------|---------|--------|------|---------|------|---------|---------|--------|--------|
| 网关类过载保护 | 达到过载保护机制的条件(产线自定义)，实现“客户业务优先放行+重要数据不丢失” 1. 对所有非核心服务永久熔断（直至过载解除），仅保留核心服务（优先保障客户业务不中断）； 2. 启用“过载保护模式”，关闭网关非必要功能（如日志详细打印、监控指标采集降级），提升网关自身处理性能； 3. 对核心接口流量采用“灰度放行”机制（按流量的优先级放行），避免重要数据丢失 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 终端类过载保护 | 达到过载保护机制的条件(产线自定义)，实现“核心功能独占资源+非核心功能强制关闭” 1. 强制关闭所有非核心功能（直至过载解除），将CPU、内存资源优先分配给核心功能（优先保障客户业务不中断）； 2. 核心请求启用“本地队列缓存”，当服务端响应超时，将请求缓存至本地（持久化存储），待过载解除后自动重试； 3. 向用户、平台推送“服务繁忙，仅核心功能可用”明确提示，禁止用户发起非核心操作。 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 平台类过载保护 | 达到过载保护机制的条件(产线自定义)，实现“客户业务优先放行+重要数据不丢失” 1. 对所有非核心服务永久熔断（直至过载解除），仅保留核心服务（优先保障客户业务不中断）； 2. 启用“过载保护模式”，关闭平台非必要功能（如日志详细打印、监控指标采集降级），提升平台自身处理性能； 3. 对核心数据采用“灰度放行”机制（按数据的优先级放行），避免重要数据丢失 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 过载保护可配置化 | 所有过载保护阈值、策略（如熔断时长、扩容数量、功能降级清单等）需支持配置化修改，且配置支持热加载，无需重启服务/终端 | 中 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 资源冲突保护 | IP地址冲突： 当系统内部不同的节点之间、内部节点与外部节点之间产生IP地址冲突的时候，业务软件层面能够检测到并且在运行日志中记录详细的故障信息（冲突节点名称、冲突的网口、冲突的IP地址等），并在告警平台上报IP地址冲突的告警 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 资源冲突保护 | MAC地址冲突： 当系统内部不同的节点之间、内部节点与外部节点之间产生MAC地址冲突的时候，业务软件层面能够检测到并且在运行日志中记录详细的故障信息（冲突节点名称、冲突的网口、冲突的MAC地址等），并在告警平台上报MAC地址冲突的告警 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |

## 系统高危操作整改

| 子类 | 具体场景 | 优先级 | 基线 | 适用产品 | 版本 | 是否满足 | 当前情况 | 工作量 | 确认人 |
|------|---------|--------|------|---------|------|---------|---------|--------|--------|
| 后台高危操作限制 | 设备的默认运维操作账号要有权限限制，不能直接操作高危操作，但是可留有提权接口，提权要做记录并可追溯。 来源： 一、公司的统一方案：https://wiki.sangfor.com/pages/viewpage.action?pageId=44773809 1. 普通的运维场景： A. ssh/console/串口登录后，默认进入权限受限终端 B. 受限终端下，只能操作非关键目录和文件，不能执行特权命令和系统调用受限终端下，能执行基础运维操作。包括 ifconfig、ping、ssh、scp、dmesg 等操作（具体由产线自行定义）。 C.支持从设备下载文件。 D.支持将文件上传到设备指定受控位置。 2. 提权场景：提供 企微 扫描提权组件，支持提权到 root 执行命令（特殊技术支持时使用） A. 扫描有申请和记录。 B. 提权下执行的命令有审计记录 3. 流程：流程约束普通运维只能使用受限终端。 4. 业务适配：依赖 ssh 操作设备的相关业务影响评估和业务适配（如 acheck、集群运维 等） | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 高危操作设计准则 | 1. 除了系统强制机制外，系统避免自动删除为用户提供业务的系统对象（例如存储池、客户端、系统配置等）和用户创建的业务对象（例如主机组、对象、文件目录、业务配置等），如发现一致性校验结果异常则上报告警通知人工介入处理 2.所有在界面上或者命令行提供的对系统对象（例如主机、集群、存储池、客户端等）或者业务对象（例如卷，对象，桶，文件目录等）的变更类（删除、移除、修改等）的高危操作都需要用户的二次确认（切默认按钮为取消），必要时需要输入验证码或者密码，并记录详细的操作日志。二次确认要清晰，需要明确影响范围，影响对象，影响后果，如“此操作将影响3台虚报机（需要列出具体的虚拟机列表）”，“此操作将格式化存储且数据不可恢复，当前有XX虚拟机引用本存储” 3. 系统内部不建议出现一次性的针对大量数据的查询操作（数据量较大建议采用分批的方式进行），如果在界面或者命令行提供给用户操作的涉及海量数据的查询类操作，则建议结果返回采用分批返回的方式，不能影响其他的业务操作 4. 具有删除（删除类的例如rm、mv、delet等等，格式化类例如mkfs等，覆盖写类的如dd等，停止类例如halt、poweroff、reboot等等）性质的操作系统命令，其操作内容（如果有的话）要用全路径（绝对路径），同时要进行命令封装，规避高风险删除场景。 5、关键数据或者配置，在被引用或者占用的情况下，不允许用户删除。例如当虚拟机运行时，禁止删除虚拟机所使用的存储/虚拟磁盘 | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |
| 高危操作可恢复 | 关键的配置类（如系统配置、网络配置、用户认证配置）、数据类资源（安全日志、虚拟机磁盘）， 在系统可容纳的范围内，需要提供备份和恢复机制，便于误操作后快速补救，例如：虚拟机回收站、配置自动备份、 如果超出系统可容纳范围，也应支持和提醒用户使用外部备份机制。例如：备份配置到FTP | 高 | DFX基线 | 全产线 | V1.0.5 |  |  |  |  |

## 数据中心和业务可靠性

| 子类 | 具体场景 | 优先级 | 基线 | 适用产品 | 版本 | 是否满足 | 当前情况 | 工作量 | 确认人 |
|------|---------|--------|------|---------|------|---------|---------|--------|--------|
| 跨数据中心多活/主备 | 对于可靠性SLA达到99.99%的业务需具备跨数据中心多活；对于可靠性SLA达到99.9%的业务需具备跨数据中心主备。如：SIA/SWG、sangforone。其他要求相对不高的业务，支持数据和配置的异地备份即可。 1.支持通过在不同的机房部署两套或者多套系统，实现配置和数据的实时同步，避免只部署一套系统或者在同一机房部署多套系统，出现服务器故障、园区运营商线路中断、区域性电力中断、自然灾害（地震、火灾）等，导致整个系统也是不可用的。如：对于系统或业务的数据库或配置中心，采用主从复制的方式，将数据和配置由一个机房A同步到另一个机房B。一旦A机房发生故障，可以将B机房的从库提升为主库，B机房继续对外提供服务。 2、跨省网络质量目前运营商不保障，有网络要求严格的业务需要考虑数据中心的选址 | 中 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 业务多线路（跨运营商） | 可以同时接入多个不同的网络运营商线路，提高网络的稳定性和可用性，当某条运营商线路出现故障时，及时切换到其他营运商线路中。 1、线路故障包扩线路中断、DDOS攻击，特别是DDOS攻击，即使买了流量清洗，但是由于机房的相同线路公用出口带宽，其它公司的业务被攻击造成机房出口被打满同样会影响业务，应优先考虑线路切换规避。 2、跨运营商访问目前网络质量差，需要考虑该场景，支持多运营商接入（或者BGP） | 中 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 运维线路独立 | 运维线路和业务线路分开，避免运维和业务线路干扰导致相互受影响，可能存在运维通道和业务都异常的情况，监控告警也可能无法及时发现和介入处理。 1、运维系统和业务系统采用同一线路出口 2、运维和业务部署在同一套HCI上面未做网口隔离 | 中 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 数据异地备份 | 系统核心数据能实现异地定时备份，如广州数据中心核心数据定期同步到上海，进行备份。如数据中心出现故障后，还能恢复客户数据。 | 高 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 配置异地备份 | 系统配置能实现异地定时备份，如广州数据中心系统配置定期同步到上海，进行备份。如数据中心出现故障后，还能恢复客户配置。 | 高 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 关键业务场景监控和处置预案 | 对客户可感知的核心业务场景进行端对端监控告警，并有对应的处置预案。 如：MSS日志时延场景，涉及的链路长（MSS、XDR、AI、数据湖、HCI等），需要对整个链路的核心节点进行监控，出现日志时延，能及时发现哪个环节的问题，并快速进行处置。提升整个响应的MTTR时间。 | 中 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 容错能力（熔断、流控、降级能力） | 产品的服务接口，在超过一定的性能压力下，服务能对外部的调用进行容错能力，降低业务的影响面。 | 中 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 容错能力（熔断、流控、降级能力） | 平台不能被组件的异常，影响全网租户。如MongoDb查询的熔断或流控机制。 | 中 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 容错能力（熔断、流控、降级能力） | 流控：即流量控制，根据流量、并发线程数、响应时间等指标，保证系统在流量突增情况下的可用性，避免系统被瞬时的流量高峰或者业务异常冲垮，一旦达到阈值则进行拒绝服务、排队等降级操作，比如数据库查询、日志入库等。 | 中 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 容错能力（熔断、流控、降级能力） | 熔断：当下游服务发生一定数量的失败后，触发熔断，后续请求就快速失败。一段时间过后再判断下游服务是否已恢复正常，从而决定是否重置熔断，比如缓存失效场景。 | 中 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 容错能力（熔断、流控、降级能力） | 降级：当访问量剧增、服务出现异常或者非核心服务影响到核心流程时，暂时牺牲掉一些东西，以保障整个系统的平稳运行。特别是SLA无法保障的第三方业务，需要有多通道和业务降级设计，比如短信验证码功能。 | 中 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |
| 容量 | 业务需要明确知道设计和部署容量，通过压测明确开局资源以及租户与资源间的关系，设定合理的监控阈值与扩容预案。例：资源与租户 / 用户量关系：最小开局（CPU/内存/空间/GPU）可支持[X]租户/用户量，需要[X]（核/GB/TB /卡）完成开局部署，每增加[Y]租户 / 用户，大约需要增加[Y]（核/GB/TB /卡） 来保证系统的稳定运行和响应速度，资源使用率告警阈值 X%。扩容预案为告警时扩容 [Z]（数量）（服务器/内存/磁盘空间/显卡）。 | 高 | DFX基线 | XaaS类 | V1.0.0 |  |  |  |  |

---

> **文档结束**
